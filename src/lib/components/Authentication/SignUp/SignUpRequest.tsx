import React from 'react';
import {Typography} from '@material-ui/core';

const SignUpRequest: React.FC = () => {
  return (
    <Typography>SignUpRequest</Typography>
  );
};

export default SignUpRequest;
