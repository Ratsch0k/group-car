describe('groupSelectors', () => {
  it.todo('getSelectedGroup returns selected group');

  describe('getGroupCars', () => {
    it.todo('if group selected, returns cars');

    it.todo('if no group selected, returns undefined');
  });

  describe('getNotSelectedGroups', () => {
    it.todo('if group selected, returns alls groups except selected');

    it.todo('if no group selected, returns all groups');
  });

  describe('getMembers', () => {
    it.todo('if group selected, returns members');

    it.todo('if no group selected, returns undefined');
  });

  describe('getGroupInvites', () => {
    it.todo('if group selected, returns invites');

    it.todo('if no group selected, returns undefined');
  });
});
